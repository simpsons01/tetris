export enum CubeRenderShape {
  First = 'first',
  Second = 'second',
  Third = 'third',
  Forth = 'forth'
}

export enum Cube {
  I,
  J,
  L,
  O,
  S,
  T,
  Z
}

export const CubeDistance = 10

export enum Canvas {
  Width = 300,
  Height = 600
}


export enum BlockState {
  Unfilled,
  Filled
}